
# handle wrong owner when dir is created on the host
sudo chown www-data. /var/www/nvm-volume

# separate nvm data from nvm itself
export NVM_DIR="/var/www/nvm-volume"
[ -s "/var/www/.nvm/nvm.sh" ] && . "/var/www/.nvm/nvm.sh"
